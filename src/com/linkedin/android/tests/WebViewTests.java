package com.linkedin.android.tests;


public class WebViewTests extends BaseTestCase {
    // CONSTANTS ------------------------------------------------------------

    // PROPERTIES -----------------------------------------------------------

    // CONSTRUCTORS ---------------------------------------------------------
    public WebViewTests() {
        super(WebViewTests.class.getSimpleName());
    }

    // METHODS --------------------------------------------------------------
    public void test_it() {
        /*startTest("12345678", "webview");
        disableLogoutAtEndForCurrentTest();
        ScreenYou.go_to_you("user1@correo.linkedinlabs.com", "crazyman");
        ScreenYou.you_tap_profile_edit();
        WaitActions.delay(5);
        WebView browser = WebViewHandler.getInstanceOfWebView();
        Logger.i(WebViewHandler.getHTML(browser));
        // demo click on Add a new job
        WebViewHandler.clickOnClassNameInWebView(browser, "action add-new-section", 
                "Add a new job");
        WaitActions.delay(5);
        passTest();*/
    }
}

